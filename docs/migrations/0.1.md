# Migrating within 0.1

`0.1.0-beta.1` is the first development baseline, so there is no earlier SDK release to
migrate from. During the 0.x line, patch releases preserve source and binary compatibility;
minor releases may make documented breaking changes here.
